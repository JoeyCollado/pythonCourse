# list = used to store multiple items in a single variable

food = ["pizza","burger","taco","shawarma"]

#food[0] = "sushi" #this will replace the item at index 0 with the new value

#print(food) #print all item on the list
#print(food[1]) #to access specific item using the square bracket to specify the index, order always starts with 0

for x in food: #prints all the item or elements on the list using loop
    print(x)